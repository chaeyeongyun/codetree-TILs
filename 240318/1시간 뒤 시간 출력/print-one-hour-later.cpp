#include <iostream>
#include <string>

int main() {
    // 여기에 코드를 작성해주세요.
    // 특정 문자를 사이에 두고 입력으로 들어가는 경우 사용 가능한 트릭
    int h, m;
    char e;
    std::cin >> h >> e >> m;
    std::cout << h << ":" << m;
    return 0;
}